@model IEnumerable<Cineverse.Models.PregledKarata>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QRKod)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.KorisnikId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QRKod)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.KorisnikId)
            </td>
                <td>
                    @if (User.IsInRole("Administrator, Korisnik, Kino radnik"))
                    {
                        <a asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                        <nobr>|</nobr>
                        <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                        <nobr>|</nobr>
                        <a asp-action="Details" asp-route-id="@item.Id">Details</a>
                        <nobr>|</nobr>
                    }
                </td>
        </tr>
}
    </tbody>
</table>
